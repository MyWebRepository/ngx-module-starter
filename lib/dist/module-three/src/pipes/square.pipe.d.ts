import { PipeTransform } from '@angular/core';
/**
 * The SquarePipe converts a number to its square
 */
export declare class SquarePipe implements PipeTransform {
    transform(value: number): number;
}
